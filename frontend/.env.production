# Production environment — used during `docker build`
# Values are injected via docker-compose build args / runtime env
NEXT_PUBLIC_API_URL=https://insurance.careerxera.com/api
NEXT_PUBLIC_APP_NAME=SunischistInsurance
